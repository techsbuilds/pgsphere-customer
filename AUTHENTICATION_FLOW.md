# Authentication Flow Diagram

## 🔐 Complete Authentication Flow

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         USER LOGIN FLOW                                  │
└─────────────────────────────────────────────────────────────────────────┘

1. USER ENTERS CREDENTIALS
   ↓
┌──────────────────────┐
│   LoginForm.tsx      │
│   - email            │
│   - password         │
│   - userType         │
└──────────────────────┘
   ↓
   dispatch(loginCustomer({ email, password }))
   ↓
┌──────────────────────────────────────────────────────────────────────────┐
│                    authSlice.ts - loginCustomer()                        │
│                                                                          │
│  POST /api/auth/customer/sign-in                                        │
│  {                                                                       │
│    "email": "user@example.com",                                         │
│    "password": "password123",                                           │
│    "userType": "Customer"                                               │
│  }                                                                       │
└──────────────────────────────────────────────────────────────────────────┘
   ↓
┌──────────────────────────────────────────────────────────────────────────┐
│                         API RESPONSE                                     │
│                                                                          │
│  {                                                                       │
│    "message": "Login Successfully",                                     │
│    "data": {                                                            │
│      "token": "eyJhbGci...JWT_TOKEN",                                  │
│      "userId": "68e1755524f31c723ff831ac",                             │
│      "userType": "Customer",                                            │
│      "pgcode": "PG92D305"                                               │
│    },                                                                    │
│    "success": true                                                       │
│  }                                                                       │
└──────────────────────────────────────────────────────────────────────────┘
   ↓
   loginCustomer.fulfilled
   ↓
┌──────────────────────────────────────────────────────────────────────────┐
│              EXTRACT & STORE (authSlice.ts)                              │
│                                                                          │
│  const { token, userId, userType, pgcode } = action.payload.data        │
│                                                                          │
│  Redux State:                      localStorage:                         │
│  ├─ state.token = token            ├─ token = "eyJhbGci..."            │
│  └─ state.user = {                 ├─ userId = "68e175..."             │
│      id: userId,                   ├─ userType = "Customer"             │
│      userId: userId,               └─ pgcode = "PG92D305"               │
│      userType: userType,                                                 │
│      pgcode: pgcode                                                      │
│    }                                                                     │
└──────────────────────────────────────────────────────────────────────────┘
   ↓
   isAuthenticated = true
   ↓
┌──────────────────────┐
│   App.tsx Routing    │
│   Detects user is    │
│   authenticated      │
└──────────────────────┘
   ↓
   REDIRECT TO /dashboard
   ↓
┌──────────────────────────────────────────────────────────────────────────┐
│                           DASHBOARD PAGE                                 │
│                                                                          │
│   ✅ User is authenticated                                              │
│   ✅ Token stored in localStorage                                       │
│   ✅ User data available in Redux                                       │
│   ✅ Ready to make authenticated API calls                              │
└──────────────────────────────────────────────────────────────────────────┘




┌─────────────────────────────────────────────────────────────────────────┐
│                    AUTHENTICATED API CALL FLOW                           │
└─────────────────────────────────────────────────────────────────────────┘

1. COMPONENT NEEDS DATA
   ↓
┌──────────────────────┐
│   Any Component      │
│   (Dashboard.tsx,    │
│   Complaints.tsx,    │
│   etc.)              │
└──────────────────────┘
   ↓
   import { authenticatedGet } from '../utils/apiHelper'
   ↓
   const data = await authenticatedGet('/api/daily-updates')
   ↓
┌──────────────────────────────────────────────────────────────────────────┐
│                     apiHelper.ts - authenticatedGet()                    │
│                                                                          │
│  1. Get token from localStorage:                                        │
│     const token = localStorage.getItem('token')                         │
│                                                                          │
│  2. Check token exists:                                                 │
│     if (!token) throw Error('No token')                                 │
│                                                                          │
│  3. Make request with Authorization header:                             │
│     fetch(url, {                                                        │
│       headers: {                                                        │
│         'Authorization': `Bearer ${token}`,                             │
│         'Content-Type': 'application/json'                              │
│       }                                                                  │
│     })                                                                   │
└──────────────────────────────────────────────────────────────────────────┘
   ↓
┌──────────────────────────────────────────────────────────────────────────┐
│                         HTTP REQUEST                                     │
│                                                                          │
│  GET /api/daily-updates                                                 │
│  Headers:                                                               │
│    Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...       │
│    Content-Type: application/json                                       │
└──────────────────────────────────────────────────────────────────────────┘
   ↓
   BACKEND VALIDATES TOKEN
   ↓
┌──────────────────────────────────────────────────────────────────────────┐
│                         API RESPONSE                                     │
│                                                                          │
│  {                                                                       │
│    "data": [...daily updates...],                                       │
│    "success": true                                                       │
│  }                                                                       │
└──────────────────────────────────────────────────────────────────────────┘
   ↓
   RETURN DATA TO COMPONENT
   ↓
   COMPONENT RENDERS DATA




┌─────────────────────────────────────────────────────────────────────────┐
│                    SESSION PERSISTENCE FLOW                              │
└─────────────────────────────────────────────────────────────────────────┘

USER LOGS IN → Token stored in localStorage
   ↓
USER CLOSES TAB
   ↓
localStorage PERSISTS (not cleared)
   ↓
USER REOPENS TAB/BROWSER
   ↓
┌──────────────────────┐
│   App.tsx Loads      │
└──────────────────────┘
   ↓
   useEffect(() => {
     dispatch(restoreSession())
   }, [])
   ↓
┌──────────────────────────────────────────────────────────────────────────┐
│              authSlice.ts - restoreSession()                             │
│                                                                          │
│  1. Read from localStorage:                                             │
│     const token = localStorage.getItem('token')                         │
│     const userId = localStorage.getItem('userId')                       │
│     const userType = localStorage.getItem('userType')                   │
│     const pgcode = localStorage.getItem('pgcode')                       │
│                                                                          │
│  2. If all exist, restore Redux state:                                  │
│     state.token = token                                                 │
│     state.user = { id: userId, userId, userType, pgcode }               │
└──────────────────────────────────────────────────────────────────────────┘
   ↓
   isAuthenticated = true
   ↓
   USER STAYS LOGGED IN ✅
   ↓
   REDIRECT TO /dashboard




┌─────────────────────────────────────────────────────────────────────────┐
│                         LOGOUT FLOW                                      │
└─────────────────────────────────────────────────────────────────────────┘

USER CLICKS LOGOUT
   ↓
┌──────────────────────┐
│   Component          │
│   const { logout }   │
│     = useAuth()      │
│   logout()           │
└──────────────────────┘
   ↓
   dispatch(logout())
   ↓
┌──────────────────────────────────────────────────────────────────────────┐
│                   authSlice.ts - logout()                                │
│                                                                          │
│  1. Clear Redux state:                                                  │
│     state.user = null                                                   │
│     state.token = null                                                  │
│                                                                          │
│  2. Clear localStorage:                                                 │
│     localStorage.removeItem('token')                                    │
│     localStorage.removeItem('userId')                                   │
│     localStorage.removeItem('userType')                                 │
│     localStorage.removeItem('pgcode')                                   │
└──────────────────────────────────────────────────────────────────────────┘
   ↓
   isAuthenticated = false
   ↓
┌──────────────────────┐
│   ProtectedRoute     │
│   Detects no auth    │
└──────────────────────┘
   ↓
   REDIRECT TO /login




┌─────────────────────────────────────────────────────────────────────────┐
│                    PROTECTED ROUTE FLOW                                  │
└─────────────────────────────────────────────────────────────────────────┘

USER TRIES TO ACCESS /dashboard
   ↓
┌──────────────────────────────────────────────────────────────────────────┐
│                      ProtectedRoute.tsx                                  │
│                                                                          │
│  const { isAuthenticated } = useAuth()                                  │
│                                                                          │
│  if (!isAuthenticated) {                                                │
│    return <Navigate to="/login" replace />                              │
│  }                                                                       │
│                                                                          │
│  return <>{children}</>  // Render dashboard                            │
└──────────────────────────────────────────────────────────────────────────┘
   ↓
   CHECK isAuthenticated
   ↓
   ┌─────────────┐              ┌─────────────┐
   │ NO TOKEN    │              │ HAS TOKEN   │
   └─────────────┘              └─────────────┘
        ↓                              ↓
   REDIRECT TO LOGIN            ALLOW ACCESS
                                      ↓
                               RENDER DASHBOARD




┌─────────────────────────────────────────────────────────────────────────┐
│                        STATE DIAGRAM                                     │
└─────────────────────────────────────────────────────────────────────────┘

            ┌─────────────────┐
            │  NOT LOGGED IN  │
            │  token = null   │
            └─────────────────┘
                    │
                    │ Login Success
                    ↓
            ┌─────────────────┐
            │   LOGGED IN     │
            │   token = xxx   │
            └─────────────────┘
                    │
        ┌───────────┼───────────┐
        │           │           │
        │           │           │
   Page Reload  API Calls    Logout
        │           │           │
        │           │           ↓
        │           │    ┌─────────────────┐
        │           │    │  NOT LOGGED IN  │
        │           │    │  token = null   │
        │           │    └─────────────────┘
        │           │
        │           ↓
        │    ┌─────────────────┐
        │    │   API Request   │
        │    │   with Bearer   │
        │    │   Token         │
        │    └─────────────────┐
        │                      │
        ↓                      ↓
   ┌─────────────────┐  ┌──────────────┐
   │  Session        │  │   Success    │
   │  Restored       │  │   Response   │
   │  from           │  └──────────────┘
   │  localStorage   │
   └─────────────────┘
        │
        ↓
   ┌─────────────────┐
   │  LOGGED IN      │
   │  token = xxx    │
   └─────────────────┘




┌─────────────────────────────────────────────────────────────────────────┐
│                         KEY COMPONENTS                                   │
└─────────────────────────────────────────────────────────────────────────┘

📁 src/store/slices/authSlice.ts
   ├─ loginCustomer()       → Handles login API call
   ├─ restoreSession()      → Restores session from localStorage
   ├─ logout()              → Clears all auth data
   └─ User state            → Stores token, userId, userType, pgcode

📁 src/utils/apiHelper.ts
   ├─ authenticatedGet()    → GET with Bearer token
   ├─ authenticatedPost()   → POST with Bearer token
   ├─ authenticatedPut()    → PUT with Bearer token
   ├─ authenticatedDelete() → DELETE with Bearer token
   └─ authenticatedFormData() → File upload with Bearer token

📁 src/hooks/useAuth.ts
   ├─ isAuthenticated       → Boolean check for auth status
   ├─ user                  → Current user data
   ├─ token                 → Current JWT token
   └─ logout()              → Logout function

📁 src/components/auth/ProtectedRoute.tsx
   └─ Route protection       → Redirects if not authenticated

📁 src/components/auth/LoginForm.tsx
   └─ Login UI              → Handles user login

📁 src/App.tsx
   ├─ Session restoration   → Calls restoreSession() on load
   └─ Routing logic         → Handles redirects based on auth status




┌─────────────────────────────────────────────────────────────────────────┐
│                      SECURITY CHECKLIST                                  │
└─────────────────────────────────────────────────────────────────────────┘

✅ Token stored in localStorage (accessible to JavaScript)
✅ Token sent via Authorization header (not in URL)
✅ Token automatically included in all authenticated requests
✅ Token cleared on logout
✅ Protected routes check authentication before rendering
✅ API calls fail gracefully if token is missing
✅ Session persists across page reloads
✅ Session cleared when user logs out

🔒 PRODUCTION RECOMMENDATIONS:
   - Implement token refresh mechanism
   - Add token expiration handling
   - Consider httpOnly cookies instead of localStorage
   - Add CSRF protection
   - Implement rate limiting on backend
   - Add request timeout handling
   - Log security events


---

For implementation details, see:
- [AUTHENTICATION_GUIDE.md](./AUTHENTICATION_GUIDE.md)
- [AUTHENTICATION_IMPLEMENTATION.md](./AUTHENTICATION_IMPLEMENTATION.md)
- [MIGRATION_EXAMPLE.md](./MIGRATION_EXAMPLE.md)
